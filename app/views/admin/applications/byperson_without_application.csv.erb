First Name,Last Name,Phone,Email Address
<%- @candidates and @candidates.each do |c|
      next unless p = c.person
-%>
<%= [:first_name, :last_name, :phone_number, :email].collect{|s| p.send(s)}.join(',') %>
<%- end -%>
