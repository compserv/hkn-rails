<table>
  <tr>
    <th>Name</th>
    <th>Fun Events</th>
  </tr>
<% @people_array.each do |person, count| %>
  <tr>
    <td><%= person.full_name %></td>
    <td><%= count %></td>
  </tr>
<% end %>
</table>
