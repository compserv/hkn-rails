<%= form_for(@dept_tour_request) do |f| %>
  <% if @dept_tour_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dept_tour_request.errors.count, "error") %> prohibited this dept_tour_request from being saved:</h2>

      <ul>
      <% @dept_tour_request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
