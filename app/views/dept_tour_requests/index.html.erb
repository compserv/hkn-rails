<h1>Listing dept_tour_requests</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @dept_tour_requests.each do |dept_tour_request| %>
  <tr>
    <td><%= link_to 'Show', dept_tour_request %></td>
    <td><%= link_to 'Edit', edit_dept_tour_request_path(dept_tour_request) %></td>
    <td><%= link_to 'Destroy', dept_tour_request, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Dept tour request', new_dept_tour_request_path %>
