window.onload = function() {
    jQuery.ajax("<%= Rails.application.routes.url_helpers.coursesurveys_chartinfo_path %>", {success: function(data) {
        generateChart(data, $("#cs"), 2);
        generateChart(data, $("#ee"), 1);
    }});
};
