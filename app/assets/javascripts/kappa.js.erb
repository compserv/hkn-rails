var KAPPA = "<img src='<%= asset_path('icons/kappa.png') %>' alt='Kappa'>"

function kappa_helper(node) {
    var new_html = $(node).html().replace('Kappa', KAPPA)
    $(node).html(new_html)
    $(node).children().each(function () {
        kappa_helper($(this))
    })
}

function kappa(node) {
    kappa_helper($('body'))
}

$(document).ready(kappa)
