# author: richardxia

You need to run these scripts on the server with the Django codebase. They will
output JSON encoded files which will serve as the inputs to the import scripts.
You also need to run them with sudo or as www-data.
